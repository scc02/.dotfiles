# 仅给 -L popup 使用的精简配置（避免嵌套时 root 的 C-. 等绑定干扰）
set -g default-command /opt/homebrew/bin/fish
set -g default-shell /opt/homebrew/bin/fish
set -g mouse on
set -g status off
set -g escape-time 0
set -g prefix C-.
unbind C-b

# Ctrl+g：关闭浮层（session 仍保留）
bind -n C-g detach-client
